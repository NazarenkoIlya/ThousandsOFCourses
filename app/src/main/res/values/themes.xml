<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ThousandsOFCourses" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ThousandsOFCourses" parent="Base.Theme.ThousandsOFCourses" />

    <style name="Theme.EditText" parent="android:Widget.EditText">
        <item name="android:background">@drawable/light_gray_background</item>
        <item name="android:textColorHint">@color/light_gray1</item>
        <item name="android:paddingStart">@dimen/edittext_padding_start</item>
        <item name="android:paddingTop">@dimen/edittext_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/edittext_padding_vertical</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="android:textSize">@dimen/edittext_text_size</item>
        <item name="android:textColor">@color/light_gray1</item>
        <item name="android:layout_marginTop">@dimen/edittext_margin_vertical</item>
        <item name="android:layout_marginBottom">@dimen/edittext_margin_vertical</item>
    </style>

    <style name="GrayActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/light_gray</item>
    </style>
</resources>